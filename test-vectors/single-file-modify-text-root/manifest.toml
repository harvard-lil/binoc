[vector]
name = "single-file-modify-text-root"
description = "Text file compared directly (file-to-file, not via directory)"
tags = ["text", "single-file", "modify"]
root_file = "story.txt"

[expected]
root_action = "modify"
has_tags = ["binoc.content-changed", "binoc.lines-added", "binoc.lines-removed"]
