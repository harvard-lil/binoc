[vector]
name = "toml-value-change"
description = "A TOML value changes; transcoded to a structured_document and reported as a value change"
tags = ["toml", "structured-document", "value-change"]

[expected]
root_action = "modify"
has_tags = ["binoc.content-changed", "binoc.document-value-change"]
