[vector]
name = "csv-rename-modify"
description = "CSV renamed and modified: detected as a single move by fuzzy correlation"
tags = ["csv", "fuzzy-move", "rename-modify"]

[expected]
has_tags = ["binoc.move", "binoc.move.modified"]
