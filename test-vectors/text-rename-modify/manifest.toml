[vector]
name = "text-rename-modify"
description = "Text file renamed and lines added: detected as a single move with content diff via fuzzy correlation"
tags = ["text", "fuzzy-move", "rename-modify"]

[expected]
has_tags = ["binoc.move", "binoc.move.modified", "binoc.content-changed"]
