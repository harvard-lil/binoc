[vector]
name = "jsonl-row-addition"
description = "JSONL stream of like-shaped objects parsed as a table; a record is appended"
tags = ["jsonl", "records", "tabular", "row-addition"]

[expected]
root_action = "modify"
has_tags = ["binoc.row-addition"]
