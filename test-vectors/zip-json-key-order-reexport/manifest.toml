[vector]
name = "zip-json-key-order-reexport"
description = "JSON files inside zip expansion get parsed and rendered as serialization-only changes"
tags = ["zip", "json", "serialization", "key-order"]

[expected]
root_action = "modify"
has_tags = ["binoc.serialization-change", "binoc.document-serialization-change"]
