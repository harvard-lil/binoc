[vector]
name = "csv-row-insertion-suppression-sentinels"
description = "Inserted rows with disclosure sentinels in count cells are reported as row additions, not value suppression"
tags = ["csv", "row-addition", "value-suppression-regression"]

[expected]
root_action = "modify"
has_tags = ["binoc.row-addition"]
