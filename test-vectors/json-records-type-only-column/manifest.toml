[vector]
name = "json-records-type-only-column"
description = "Typed JSON records re-serialized with one numeric column as strings; canonical values match"
tags = ["json", "records", "tabular", "column-type-change"]

[expected]
root_action = "modify"
has_tags = ["binoc.column-type-change", "binoc.schema-change"]
