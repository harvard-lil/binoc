[vector]
name = "ion-value-change"
description = "An Ion text scalar value changes; transcoded to a structured_document and reported as a value change"
tags = ["ion", "structured-document", "value-change"]

[expected]
root_action = "modify"
has_tags = ["binoc.content-changed", "binoc.document-value-change"]
