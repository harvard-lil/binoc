[vector]
name = "plist-value-change"
description = "An XML plist scalar value changes; transcoded to a structured_document and reported as a value change"
tags = ["plist", "structured-document", "value-change"]

[expected]
root_action = "modify"
has_tags = ["binoc.content-changed", "binoc.document-value-change"]
