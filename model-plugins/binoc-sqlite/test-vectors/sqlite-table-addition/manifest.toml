[vector]
name = "sqlite-table-addition"
description = "B has an extra table"
tags = ["sqlite", "table-addition"]

[expected]
root_action = "modify"
# An added table is now an added child node under the database container.
has_tags = ["binoc.content-changed"]
