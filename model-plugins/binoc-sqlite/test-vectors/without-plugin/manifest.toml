# Same snapshots as sqlite-row-addition but without the SQLite rule pack.
[vector]
name = "without-plugin"
description = "SQLite DBs compared by byte-level fallback (no SQLite rule pack)"
tags = ["sqlite", "without-plugin"]

[expected]
root_action = "modify"
# Binary writer will see content change.
has_tags = ["binoc.content-changed"]
