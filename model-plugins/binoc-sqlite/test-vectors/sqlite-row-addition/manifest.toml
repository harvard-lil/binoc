[vector]
name = "sqlite-row-addition"
description = "Same schema, one more row in B"
tags = ["sqlite", "row-addition"]

[expected]
root_action = "modify"
# Row changes now render on the table child node via the stdlib tabular writer.
has_tags = ["binoc.row-addition"]
