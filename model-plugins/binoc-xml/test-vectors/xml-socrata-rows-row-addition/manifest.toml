[vector]
name = "xml-socrata-rows-row-addition"
description = "A Socrata-style rows.xml with repeated <row> record elements gains a row; repeated identically-named siblings collect into a JSON array, and the addition is reported as a document value change"
tags = ["xml", "structured-document", "value-change", "socrata", "repeated-elements"]

[expected]
root_action = "modify"
has_tags = ["binoc.content-changed", "binoc.document-value-change"]
