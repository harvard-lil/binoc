[vector]
name = "xpt-multidataset-members"
description = "Multi-member SAS XPORT files decompose into one tabular child per dataset, so a later dataset is diffed (and a new dataset added) instead of silently dropped."
tags = ["xpt", "multi-table", "row-addition", "table-addition"]

[expected]
root_action = "modify"
has_tags = [
    "binoc.content-changed",
    "binoc.row-addition",
]
