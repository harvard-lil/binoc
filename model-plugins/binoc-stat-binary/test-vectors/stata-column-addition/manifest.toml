[vector]
name = "stata-column-addition"
description = "Stata DTA files parse into the standard tabular artifact and report column additions through the tabular analyzer."
tags = ["stata", "tabular", "schema"]

[expected]
root_action = "modify"
has_tags = ["binoc.column-addition", "binoc.schema-change"]
