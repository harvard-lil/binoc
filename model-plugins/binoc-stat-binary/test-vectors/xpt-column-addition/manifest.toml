[vector]
name = "xpt-column-addition"
description = "SAS XPORT files parse into the standard tabular artifact and report column additions through the tabular analyzer."
tags = ["xpt", "tabular", "schema"]

[expected]
root_action = "modify"
has_tags = ["binoc.column-addition", "binoc.schema-change"]
