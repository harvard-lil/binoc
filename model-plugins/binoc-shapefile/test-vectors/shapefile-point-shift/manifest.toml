[vector]
name = "shapefile-point-shift"
description = "A point feature moves, growing the bounding box while the feature count and geometry type stay the same; reported as a bbox value change"
tags = ["shapefile", "structured-document", "value-change", "point"]

[expected]
root_action = "modify"
has_tags = ["binoc.content-changed", "binoc.document-value-change"]
