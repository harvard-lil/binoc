[vector]
name = "parquet-column-addition"
description = "Snapshot B adds a new column to the same rows"
tags = ["parquet", "column-addition"]

[expected]
root_action = "modify"
has_tags = ["binoc.column-addition", "binoc.schema-change"]
